const Joi = require('joi')


const paramValid = (sch) => {
    // here put your joi valid functions
}
const schemaList = {
    delete:"hi"
}

module.schemaList = schemaList
module.exports = paramValid
