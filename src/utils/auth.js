const JWT = require('jwt')


class Jwt {
    constructor(key){
        this.key = key
    }
    Check(key){
        // check function
        return
    }
    Generate(data){
        // Generate function
        return
    }
}


module.exports = Jwt
